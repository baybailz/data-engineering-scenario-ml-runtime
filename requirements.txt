duckdb==1.3.2
numpy==2.3.2
pandas==3.0.5
scikit-learn==1.7.1
skl2onnx==1.20.0
onnx==1.22.0
# protobuf 7.34 rejects the booleans skl2onnx writes into int attributes,
# which fails the ONNX export. Pinned until skl2onnx stops doing that.
protobuf==6.33.6
onnxruntime==1.29.0
pytest==9.1.1
ruff==0.16.4
